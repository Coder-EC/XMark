package xmark.util;

import java.awt.Color;

/**
 * The core utilities class in the app. Provides several basic functions.
 * Is the control center of the app. Most functions in settings are based
 * here.
 */
public interface AppUtilities {

    /** The base color scheme used in the app panel. */
    Color BASE_COLOR = new Color(0x181818);

}
